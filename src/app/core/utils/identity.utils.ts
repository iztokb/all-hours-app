/**
 * @description
 * Function responsible for token validation
 * @param {string}token
 * @returns { boolean }
 */
export const ValidateAccessToken = (token: string): boolean => {
  // TODO
  // Implement token validation
  return true;
};
